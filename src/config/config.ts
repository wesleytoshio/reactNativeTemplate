export const env = __DEV__ ? 'development' : 'production';
export const isDevelopment = env === 'development';

export const apiTimeout = 15 * 1000;

export const endPoint = 'https://localhost:3000/';